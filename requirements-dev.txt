# Production + dev requirements
esptool         >= 3.1
pyminifier      >= 2.1                  #python-minifier

# dev requirements
pytest          >= 6.2.4 
pytest-cov      >= 2.12.1
pytest-mock     >= 1.13.0
