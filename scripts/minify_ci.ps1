# FIXME: seperate script to work-around poetry install not making the commands executable / findable on CI 

# Minify all versions
# python -m stubber.stubber minify --all --compile
# or : 
poetry run stubber minify --all --compile