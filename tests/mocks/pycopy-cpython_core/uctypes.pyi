
from typing import Any, Dict, Optional, Sequence, Tuple, Union
Node = Any
def bytearray_at(addr: Any, sz: Any) -> Any: ...
    #   0: return ctypes.string_at(addr,sz)
    # ? 0: return ctypes.string_at(addr, sz)
