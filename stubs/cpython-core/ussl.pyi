
from typing import Any, Dict, Optional, Sequence, Tuple, Union
Node = Any
def wrap_socket(*args, **kw) -> Any: ...
    #   0: return uio.UioStream(s.makefile('rwb',buffering=0),is_bin=bool)
    # ? 0: return uio.UioStream(str.makefile(str, buffering=number), is_bin=bool)
